import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center",
  },
  bodyTitle: {
    fontSize: 20,
    fontWeight: "bold",
    marginBottom: 5,
  },
  bodyText: {
    fontSize: 18,
    marginBottom: 20,
  },
  buttonChar: {},
});
